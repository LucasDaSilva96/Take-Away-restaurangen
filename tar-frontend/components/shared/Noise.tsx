const Noise = () => {
  return (
    <div className="bg-noise-pattern animate-noise opacity-40 pointer-events-none bg-50 fixed z-inf w-auto h-auto -bottom-[20vw] -top-[20vw] -left-[20vw] -right-[20vw]"></div>
  );
};

export default Noise;
